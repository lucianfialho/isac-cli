# ISAC CLI does not require environment variables.
# It uses Claude Code CLI (https://claude.ai/download) as its AI engine.
#
# If you're running the example project (examples/claude-on-mars/):
# DATABASE_URL=postgresql://...
